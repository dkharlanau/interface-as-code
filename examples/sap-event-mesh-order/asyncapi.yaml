asyncapi: 3.0.0
info:
  title: SAP order created event
  version: 1.0.0
servers:
  prod:
    host: event-mesh.example
    protocol: kafka
channels:
  order.created:
    address: order.created
    messages:
      OrderCreated:
        payload:
          type: object
